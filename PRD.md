\# 项目代号：EasyClash (极简代理客户端) ## 1. 项目概述与核心理念 本项目旨在开发一款极简的桌面代理客户端，作为 Clash / Clash Verge 的“平替”。 - **核心痛点**：现有工具功能臃肿、配置复杂、对小白极不友好。 - **核心卖点**：打开开关就能代理，自动检测并连接延迟最低的服务器。隐藏所有高级配置，做到“傻瓜式”体验。 - **合规前提**：本工具仅为本地网络管理 UI 壳子，不包含任何内置节点，用户需自行导入订阅链接。 ## 2. 技术栈选型 (严格遵守) - **底层引擎**：`mihomo` (原 Clash.Meta) 核心二进制文件。绝对不要自己实现任何代理协议。 - **后端/控制层**：Go (Golang) 1.21+。负责进程管理、系统代理设置、API 交互。 - **GUI 框架**：Wails v2。用于打包原生桌面应用。 - **前端 UI**：Vue 3 (Composition API) + TypeScript + TailwindCSS。 - **状态管理**：Pinia。 ## 3. 系统架构设计 应用分为三层： 1. **底层引擎层**：内嵌或调用外部的 `mihomo` 可执行文件，加载预设的 `config.yaml`。 2. **逻辑控制层 (Go Backend)**：   - 进程生命周期管理 (启动/停止 mihomo)。   - 操作系统级全局代理设置 (System Proxy)。   - 与 mihomo RESTful API (默认 `127.0.0.1:9090`) 进行 HTTP 交互。 3. **极简 UI 层 (Vue Frontend)**：只展示核心状态，提供一键操作。 ## 4. 核心功能实现指南 (给 Cursor 的具体指令) ### 4.1 核心引擎管理 (Go Backend) - **启动**：使用 `os/exec` 启动 `mihomo` 进程，传入 `-d` 参数指定配置目录。需处理进程退出时的清理工作 (Kill 进程)。 - **配置**：内置一份极简的 `config.yaml`，开启 `external-controller: 127.0.0.1:9090`，并预设好基础的“国内直连、国外代理”分流规则。 ### 4.2 系统代理控制 (Go Backend) 实现 `SetSystemProxy(enable bool)` 方法，需根据操作系统分别处理： - **Windows**：通过修改注册表 `HKCU\Software\Microsoft\Windows\CurrentVersion\Internet Settings` 中的 `ProxyEnable` 和 `ProxyServer` (值为 `127.0.0.1:7890`，假设 mihomo mixed-port 为 7890)。 - **macOS**：通过 `os/exec` 调用 `networksetup` 命令。获取当前活跃网络接口，执行 `networksetup -setwebproxy` 和 `networksetup -setsecurewebproxy`。 - **Linux**：设置环境变量 `http_proxy` 和 `https_proxy` (注意：Linux 桌面环境差异大，优先保证 Win/Mac，Linux 可作降级处理)。 ### 4.3 mihomo API 交互 (Go Backend) 封装一个 `MihomoClient` 结构体，通过 `net/http` 调用以下 API： - `GET /proxies`：获取所有节点列表。 - `GET /group/{name}/delay`：对指定节点组进行并发延迟测试 (URL 设为 `http://www.gstatic.com/generate_204`，timeout 2000ms)。 - `PUT /proxies/{name}`：切换当前使用的节点。 - **自动测速逻辑**：遍历所有 `Selector` 或 `URLTest` 类型的组，调用 delay API，找出延迟最低的节点，并调用 PUT API 切换。 ### 4.4 极简 UI 规范 (Vue Frontend) - **主界面**：  - 中心是一个巨大的圆形“开关”按钮 (Toggle)。  - 开关下方显示当前状态：“已连接 - 🇯🇵 日本东京 (45ms)” 或 “未连接”。  - 底部一个次级按钮：“一键智能测速”。 - **系统托盘**：极简菜单，仅包含“显示主窗口”、“开启/关闭代理”、“退出”。 - **设计风格**：深色模式 (Dark Mode)，使用 TailwindCSS，要求动画流畅 (如开关切换时的过渡效果)。 ## 5. Cursor 开发执行步骤 (Step-by-Step) 请按照以下顺序为我生成代码和文件： **Phase 1: 项目初始化与基础结构** 1. 帮我生成 Wails 项目的标准目录结构说明。 2. 在 `main.go` 中初始化 Wails 应用，配置窗口大小 (宽 350, 高 500，禁止调整大小，无边框或极简边框)。 **Phase 2: Go 后端核心逻辑** 1. 创建 `backend/proxy_manager.go`，实现 mihomo 进程的启动、停止和状态检查。 2. 创建 `backend/system_proxy.go`，实现跨平台的系统全局代理开启/关闭逻辑 (使用 runtime.GOOS 判断)。 3. 创建 `backend/mihomo_api.go`，封装获取节点、测速、切换节点的 HTTP 请求方法。 4. 在 `app.go` 中暴露前端需要调用的方法：`ToggleProxy()`, `GetStatus()`, `AutoSelectBestNode()`。 **Phase 3: Vue 前端 UI 开发** 1. 配置 TailwindCSS 和 Pinia。 2. 重写 `App.vue`，实现极简的深色模式 UI，包含大开关、状态文本和测速按钮。 3. 实现 Wails 运行时绑定 (Runtime binding)，调用 Go 后端方法，并处理加载状态 (Loading) 和错误提示 (Toast)。 **Phase 4: 完善与打包** 1. 添加应用退出时的清理逻辑 (确保关闭系统代理并 Kill mihomo 进程)。 2. 提供 `wails build` 的配置建议，确保打包体积最小化。 ## 6. 约束与代码规范 - **Go 代码**：必须有完整的错误处理 (Error handling)，不要使用 `_` 忽略 error。使用 `log/slog` 进行日志记录。 - **Vue 代码**：使用 `<script setup lang="ts">`，组件必须拆分，样式全部使用 Tailwind 工具类，禁止写自定义 CSS 文件 (除非是全局动画)。 - **安全性**：mihomo API 必须绑定在 `127.0.0.1`，绝不能暴露在 `0.0.0.0`。 - **MSP 原则**：不要实现“订阅转换”、“规则编辑”、“日志查看”等复杂功能，保持代码极简。 --- **给 Cursor 的初始指令：** "我已经阅读了上述 PRD。请从 Phase 1 开始，帮我初始化 Wails 项目结构，并生成 main.go 和基础的 app.go 骨架代码。"